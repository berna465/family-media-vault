services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    restart: unless-stopped
    volumes:
      - ${VAULT_ROOT:-/srv/family-vault}/runtime/uptime-kuma/data:/app/data
    ports:
      - "3001:3001"
